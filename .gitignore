# Rust/C++ Build Artifacts
target/
bin/
obj/
*.pdb
*.log

# Driver Signing (CRITICAL: DO NOT COMMIT)
*.cer
*.pfx
*.pvk

# Deployment Metadata
*.cat
*.sys
*.inf

# Agent Artifacts
# We track the .agent directory, but ignore specific ephemeral files if they appear.
# For now, we want to track everything in .agent/
